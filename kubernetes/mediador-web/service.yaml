apiVersion: v1
kind: Service
metadata:
  name: mediador-web-service
  namespace: medisupply
spec:
  type: NodePort
  selector:
    app: mediador-web
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
      nodePort: 30501  # puerto entre 30000 y 32767
